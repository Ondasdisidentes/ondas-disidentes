-- =========================================================
-- Ondas Disidentes — portada por episodio
-- =========================================================

alter table public.episodios
  add column imagen_url text;
